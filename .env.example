# OpenAI 服务端 API key（真实值只放 .env，不要提交到 git）
OPENAI_API_KEY=YOUR_NEW_KEY_HERE

# 模型可随时替换（默认 gpt-5.6-terra）
OPENAI_MODEL=gpt-5.6-terra

# Oraculum 自己的 AI API 访问口令（不是 OpenAI key）
AI_ACCESS_TOKEN=CHANGE_ME_TO_A_PRIVATE_TOKEN

PORT=8787
